//node('build-node') {
//  myYaml = readYaml file: "./config/myYamlFile.yaml"
//}
pipeline {
   agent {label 'build-node'}
   stages {
      stage('Read YAML') {
         steps {
            script {
               def myScript = load './vars/example.groovy'
               myScript.myMethod('parameter1')
            }
         }
      }
   }
}
